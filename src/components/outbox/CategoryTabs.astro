---
interface Props {
	categories: string[];
}

const { categories } = Astro.props;

const tabs = [
	{ label: 'All', dataCategory: '' },
	...categories.map((label) => ({ label, dataCategory: label })),
];
---

<nav
	class="flex flex-wrap items-center gap-6 type-label"
	aria-label="Outbox category filter"
	data-outbox-tabs
>
	{
		tabs.map((tab, i) => (
			<button
				type="button"
				class:list={[
					'cursor-pointer border-none bg-transparent p-0 transition-colors duration-150',
					i === 0
						? 'text-text-primary'
						: 'text-text-inactive hover:text-text-third',
				]}
				data-category={tab.dataCategory}
				data-active={i === 0 ? 'true' : 'false'}
			>
				{tab.label}
			</button>
		))
	}
</nav>
