---
/**
 * A two-column grid that spaces project cards to match the Figma layout.
 * Renders its children (ProjectCard components) into a CSS grid.
 * Used by the /projects page.
 */
---

<div class="grid sm:grid-cols-2 gap-x-16 gap-8">
	<slot />
</div>
