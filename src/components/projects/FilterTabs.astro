---
interface Tab {
	label: string;
	href: string;
	active?: boolean;
}

interface Props {
	tabs: Tab[];
}

const { tabs } = Astro.props;
---

<nav class="flex items-center gap-5" aria-label="Project filters">
	{
		tabs.map((tab) => (
			<a
				class:list={[
					'text-sm font-medium no-underline transition-colors duration-150',
					tab.active
						? 'text-text hover:text-text'
						: 'text-text-inactive hover:text-text-secondary',
				]}
				href={tab.href}
			>
				{tab.label}
			</a>
		))
	}
</nav>
